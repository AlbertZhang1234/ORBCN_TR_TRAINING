# 开发规则

1. Web 路由不能直接写 SQL、操作文件或调用 SMTP。
2. Domain 不能导入 Flask、SQLite、环境变量和具体 HTTP 客户端。
3. config 只负责配置，不加载业务数据。
4. 所有依赖在入口处组装，不依靠模块级全局单例。
5. 不允许用延迟导入解决循环依赖；出现循环就重新审视模块边界。
6. Python，TypeScript，Javascript 等，以及其他语言的代码文件尽量不要超过 400 行，如果真的要超过400行，必须解释为什么不拆。
7. 单个函数超过 60 行：检查是否混合了验证、业务、持久化和外部调用。
8. 类超过 300 行：检查是否管理了多个业务实体或多张不相关的表。
9. 一个路由函数只做参数解析、调用 service、转换响应。
10. 一个模块导入十几个项目内部模块时，检查它是否已经成为协调中心。
11. 新增第二份相似规则时，先抽取公共领域函数，禁止复制后修改。
